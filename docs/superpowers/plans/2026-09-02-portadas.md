# Portadas para videos programados — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Campo `portada` (URL pública de imagen) en la API, el CSV y el editor de posts, aplicado por red: `cover_url` en el REELS de Instagram, `thumb` en bytes en el video de Facebook, `thumbnails.set` best-effort en YouTube; X y Threads la ignoran.

**Architecture:** `scheduledPosts.coverUrl` viaja del intake (batch/editor, validado y re-subido al Blob) al `PublishInput` que `run.ts` arma para cada publisher. Los helpers de validación y los builders de cuerpos son puros y testeados; las descargas y multiparts son orquestación HTTP sin test, como todo el repo.

**Tech Stack:** Next.js App Router, Drizzle + Neon (`npm run db:push` para la columna nueva), Vercel Blob, Graph API v23, YouTube Data API v3, Vitest.

**Spec:** `docs/superpowers/specs/2026-09-02-portadas-design.md`

## Global Constraints

- Frases fijas EXACTAS nuevas: `'La portada requiere un video en media.'` y `'La portada debe ser una imagen.'`; las existentes no cambian salvo `CSV_HEADER_ERROR` (Task 2). Detalle upstream solo a `console.error` truncado (`.slice(0, 300)` en publishers, `(0, 200)` en batch).
- La portada de YouTube y la de Facebook son best-effort: su fallo JAMÁS cambia el `PublishOutcome` ni sacrifica el video.
- El encabezado CSV de 4 columnas sigue siendo válido.
- Cero dependencias nuevas; driver neon-http sin transacciones.
- Correr `npx vitest run` antes de cada commit; `npm run lint` y `npx tsc --noEmit` donde el task lo pida.
- Rutas bajo `src/app/admin/(dash)/` y `[id]` van entre comillas en comandos de shell.

---

### Task 1: `coverUrl` de punta a punta (schema → PublishInput)

**Files:**
- Modify: `src/db/schema.ts` (tabla `scheduledPosts`, línea ~222)
- Modify: `src/lib/social/publish/publisher.ts` (tipo `PublishInput`)
- Modify: `src/lib/social/publish/run.ts` (función `attempt` y su llamada)

**Interfaces:**
- Produces: `scheduledPosts.coverUrl` (text nullable); `PublishInput.coverUrl: string | null`; `attempt(..., content: { caption: string; coverUrl: string | null })`.

- [ ] **Step 1: Columna en el schema**

En `src/db/schema.ts`, dentro de `pgTable('scheduled_posts', {...})`, después de `scheduledAt`:

```ts
  // Diseñada, no un fotograma: la aplican los publishers que pueden (IG/FB/YT).
  coverUrl: text('cover_url'),
```

- [ ] **Step 2: Migrar la base**

Run: `npm run db:push`
Expected: agrega la columna nullable `cover_url` sin tocar datos. (Usa el `.env.local` del worktree.)

- [ ] **Step 3: El tipo y la tubería**

En `src/lib/social/publish/publisher.ts`, `PublishInput` gana:

```ts
  /** Imagen de portada (URL del Blob) para los caminos de video; null si no hay. */
  coverUrl: string | null
```

En `src/lib/social/publish/run.ts`:
- La firma de `attempt` cambia `content: { caption: string }` → `content: { caption: string; coverUrl: string | null }`.
- La llamada existente (`publishDue`, ~línea 69-70) pasa `{ caption: target.captionOverride ?? post.caption, coverUrl: post.coverUrl }`.
- El objeto de `publisher.publish({...})` gana `coverUrl: content.coverUrl,`.

- [ ] **Step 4: Verificar y commitear**

Run: `npx vitest run && npm run lint && npx tsc --noEmit`
Expected: todo verde (el campo nuevo aún no lo consume nadie).

```bash
git add src/db/schema.ts src/lib/social/publish/publisher.ts src/lib/social/publish/run.ts
git commit -m "Agrega coverUrl del schema al PublishInput"
```

---

### Task 2: Contrato de intake — CSV, validación y persistencia del lote

**Files:**
- Modify: `src/lib/social/publish/csv.ts`
- Modify: `src/lib/social/publish/batch.ts`
- Modify: `src/app/api/schedule/batch/route.ts` (normalización del JSON)
- Test: `src/lib/social/publish/csv.test.ts`, `src/lib/social/publish/batch.test.ts`

**Interfaces:**
- Consumes: `scheduledPosts.coverUrl` (Task 1).
- Produces: `BatchItem.portada?: string`; `PORTADA_NEEDS_VIDEO`, `PORTADA_NOT_IMAGE` exportadas desde `batch.ts`.

- [ ] **Step 1: Tests que fallan — CSV**

En `src/lib/social/publish/csv.test.ts` agregar:

```ts
describe('portada en el CSV', () => {
  it('el encabezado de 5 columnas mapea la portada; celda vacía es sin portada', () => {
    const text = [
      'fecha,texto,redes,media,portada',
      '2026-09-08 19:00,"Corto 1",instagram|youtube,https://ej.com/c01.mp4,https://ej.com/c01.jpg',
      '2026-09-08 20:00,"Sin portada",threads|x,,',
    ].join('\n')
    const result = csvToBatchItems(text)
    if ('error' in result) throw new Error(result.error)
    expect(result.items[0]!.portada).toBe('https://ej.com/c01.jpg')
    expect(result.items[1]!.portada).toBe('')
  })

  it('el encabezado clásico de 4 columnas sigue vivo: portada vacía', () => {
    const text = 'fecha,texto,redes,media\n2026-09-08 19:00,Hola,threads,'
    const result = csvToBatchItems(text)
    if ('error' in result) throw new Error(result.error)
    expect(result.items[0]!.portada).toBe('')
  })

  it('cinco columnas con otro nombre final rechazan el lote', () => {
    const result = csvToBatchItems('fecha,texto,redes,media,cover\n')
    expect(result).toHaveProperty('error')
  })
})
```

(Import `csvToBatchItems` ya existe en el archivo de test.)

- [ ] **Step 2: Tests que fallan — validación del lote**

En `src/lib/social/publish/batch.test.ts` agregar (imports: sumar `PORTADA_NEEDS_VIDEO, PORTADA_NOT_IMAGE` al import de `./batch`):

```ts
describe('portada en validateBatchItem', () => {
  const conVideo = { ...base, redes: ['facebook'], media: ['https://ej.com/v.mp4'] }

  it('portada con video en media pasa', () => {
    expect(
      validateBatchItem({ ...conVideo, portada: 'https://ej.com/p.jpg' }, now),
    ).toBeNull()
  })

  it('portada sin ningún video posible es la frase fija', () => {
    expect(
      validateBatchItem({ ...base, media: ['https://ej.com/a.jpg'], redes: ['facebook'], portada: 'https://ej.com/p.jpg' }, now),
    ).toBe(PORTADA_NEEDS_VIDEO)
    expect(validateBatchItem({ ...base, portada: 'https://ej.com/p.jpg' }, now)).toBe(
      PORTADA_NEEDS_VIDEO,
    )
  })

  it('media de tipo diferido mantiene viva la portada: el content-type decidirá', () => {
    const drive = 'https://drive.usercontent.google.com/download?id=x&export=download'
    expect(
      validateBatchItem({ ...base, redes: ['facebook'], media: [drive], portada: 'https://ej.com/p.jpg' }, now),
    ).toBeNull()
  })

  it('portada con extensión de video es la otra frase fija', () => {
    expect(
      validateBatchItem({ ...conVideo, portada: 'https://ej.com/p.mp4' }, now),
    ).toBe(PORTADA_NOT_IMAGE)
  })

  it('portada vacía o ausente no exige nada', () => {
    expect(validateBatchItem({ ...conVideo, portada: '' }, now)).toBeNull()
    expect(validateBatchItem(conVideo, now)).toBeNull()
  })
})
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `npx vitest run src/lib/social/publish/csv.test.ts src/lib/social/publish/batch.test.ts`
Expected: FAIL (portada no existe en los tipos ni en el parser).

- [ ] **Step 4: Implementar — csv.ts**

```ts
export const CSV_HEADER_ERROR =
  'El encabezado del CSV debe ser exactamente: fecha,texto,redes,media — con portada opcional como quinta columna.'
```

En `csvToBatchItems`: la validación del encabezado acepta 4 o 5 columnas — las
primeras cuatro exactas como hoy, y si `header.length === 5`, `header[4]` debe ser
`'portada'` (cualquier otra cosa → `CSV_HEADER_ERROR`; `header.length` fuera de
4-5 → ídem). El mapeo de filas gana `portada: (row[4] ?? '').trim()`, y el tipo
de retorno del ítem suma `portada: string`.

- [ ] **Step 5: Implementar — batch.ts**

```ts
export type BatchItem = {
  fecha: string
  texto: string
  redes: string[]
  media: string[]
  /** URL pública de imagen; vacía o ausente = sin portada. Solo válida con video. */
  portada?: string
}

export const PORTADA_NEEDS_VIDEO = 'La portada requiere un video en media.'
export const PORTADA_NOT_IMAGE = 'La portada debe ser una imagen.'
```

En `validateBatchItem`, después del bloque de conteo de media y antes del
`return validateScheduleDraft(...)`:

```ts
  const portada = item.portada?.trim()
  if (portada) {
    if (mediaTypeFromUrl(portada) === 'video') return PORTADA_NOT_IMAGE
    // Un tipo diferido en media puede resultar video (un link de Drive), así que
    // solo se rechaza aquí cuando TODA la media es imagen segura; la re-validación
    // post-descarga da el veredicto final con los tipos reales.
    const videoPossible = item.media.some((url) => mediaTypeFromUrl(url) !== 'image')
    if (!videoPossible) return PORTADA_NEEDS_VIDEO
  }
```

En `scheduleBatch`, dentro del `try`, después del loop de descargas de media y
antes del insert del post:

```ts
      // La portada sigue la misma tubería que la media, con dos veredictos propios:
      // los tipos reales deben incluir un video, y la portada misma debe ser imagen.
      const portada = item.portada?.trim()
      let coverUrl: string | null = null
      if (portada) {
        if (!uploaded.some((m) => m.mediaType === 'video')) {
          results.push({ index, ok: false, error: PORTADA_NEEDS_VIDEO })
          continue
        }
        const stored = await mediaToBlob(portada, mediaTypeFromUrl(portada))
        if (!stored) {
          results.push({ index, ok: false, error: 'No se pudo leer una media de la fila.' })
          continue
        }
        if (stored.mediaType !== 'image') {
          results.push({ index, ok: false, error: PORTADA_NOT_IMAGE })
          continue
        }
        coverUrl = stored.url
      }
```

y el insert del post pasa a `.values({ caption: item.texto, scheduledAt, coverUrl })`.

- [ ] **Step 6: Implementar — la puerta API**

En `src/app/api/schedule/batch/route.ts`, la normalización de cada item del JSON
(donde hoy arma `fecha/texto/redes/media`) gana:

```ts
      portada: typeof raw.portada === 'string' ? raw.portada : '',
```

(ajustando `raw` al nombre real de la variable del item crudo en ese archivo).

- [ ] **Step 7: Run tests to verify they pass**

Run: `npx vitest run`
Expected: PASS completo (si algún test existente asertaba `CSV_HEADER_ERROR` con
la frase vieja, actualízalo a la nueva — es el único cambio de frase permitido).

- [ ] **Step 8: Commit**

```bash
git add src/lib/social/publish/csv.ts src/lib/social/publish/csv.test.ts src/lib/social/publish/batch.ts src/lib/social/publish/batch.test.ts src/app/api/schedule/batch/route.ts
git commit -m "Agrega la portada al contrato del lote (CSV y API)"
```

---

### Task 3: Instagram — `cover_url` en el REELS

**Files:**
- Modify: `src/lib/social/publish/instagram.ts`
- Test: `src/lib/social/publish/instagram.test.ts`

**Interfaces:**
- Consumes: `PublishInput.coverUrl` (Task 1).
- Produces: `reelContainerParams(caption, media, coverUrl: string | null)`.

- [ ] **Step 1: Tests que fallan**

En `src/lib/social/publish/instagram.test.ts` (ajustando al estilo del archivo):

```ts
describe('reelContainerParams con portada', () => {
  const media = { url: 'https://blob/v.mp4', mediaType: 'video' as const, position: 0 }

  it('con portada agrega cover_url', () => {
    expect(reelContainerParams('Hola', media, 'https://blob/p.jpg')).toEqual({
      media_type: 'REELS',
      video_url: 'https://blob/v.mp4',
      caption: 'Hola',
      cover_url: 'https://blob/p.jpg',
    })
  })

  it('sin portada el cuerpo queda como siempre', () => {
    expect(reelContainerParams('Hola', media, null)).toEqual({
      media_type: 'REELS',
      video_url: 'https://blob/v.mp4',
      caption: 'Hola',
    })
  })
})
```

Los tests existentes de `reelContainerParams` que llamen con dos argumentos deben
actualizarse a `(caption, media, null)`.

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/lib/social/publish/instagram.test.ts`

- [ ] **Step 3: Implementar**

```ts
// La portada diseñada del reel; el carrusel y la foto no la llevan — Graph solo
// la acepta en el contenedor REELS.
export function reelContainerParams(
  caption: string,
  media: PublishMedia,
  coverUrl: string | null,
): Record<string, string> {
  const params: Record<string, string> = { media_type: 'REELS', video_url: media.url, caption }
  if (coverUrl) params.cover_url = coverUrl
  return params
}
```

Y el único caller dentro de `publish` (rama de video único) pasa `input.coverUrl`.

- [ ] **Step 4: Verificar y commitear**

Run: `npx vitest run && npx tsc --noEmit`

```bash
git add src/lib/social/publish/instagram.ts src/lib/social/publish/instagram.test.ts
git commit -m "Agrega cover_url al contenedor REELS de Instagram"
```

---

### Task 4: Facebook (`thumb` en bytes) y YouTube (`thumbnails.set` best-effort)

**Files:**
- Modify: `src/lib/social/publish/facebook.ts`
- Modify: `src/lib/social/publish/youtube.ts`

**Interfaces:**
- Consumes: `PublishInput.coverUrl` (Task 1).
- Produces: nada aguas abajo.

- [ ] **Step 1: Facebook — descargar la portada y adjuntarla como `thumb`**

En `facebook.ts`, agregar sobre `postForm`:

```ts
/**
 * /videos no acepta una URL de portada — `thumb` viaja en bytes. La portada ya
 * vive en nuestro Blob; si su descarga falla, el video sale sin ella (best-effort:
 * jamás se sacrifica el video por la portada) y el motivo queda en el log.
 */
async function fetchCoverBlob(coverUrl: string): Promise<Blob | null> {
  try {
    const response = await fetch(coverUrl, { signal: AbortSignal.timeout(30_000) })
    if (!response.ok) {
      console.error('Portada de Facebook:', response.status, coverUrl.slice(0, 200))
      return null
    }
    return await response.blob()
  } catch (error) {
    console.error('Portada de Facebook:', String(error).slice(0, 300))
    return null
  }
}
```

La rama de video único cambia a:

```ts
    if (media.length === 1 && media[0]!.mediaType === 'video') {
      const params = { ...videoPostParams(input.caption, media[0]!), access_token: token }
      const cover = input.coverUrl ? await fetchCoverBlob(input.coverUrl) : null
      let data: Record<string, unknown> | null
      if (cover) {
        const form = new FormData()
        for (const [key, value] of Object.entries(params)) form.set(key, value)
        form.set('thumb', cover, 'portada.jpg')
        const response = await fetch(`${GRAPH}/${input.accountExternalId}/videos`, {
          method: 'POST',
          body: form,
        })
        if (!response.ok) {
          console.error('Facebook publish:', response.status, (await response.text()).slice(0, 300))
          data = null
        } else {
          data = await response.json()
        }
      } else {
        data = await postForm(`${GRAPH}/${input.accountExternalId}/videos`, params)
      }
      const id = data?.id
      if (typeof id !== 'string') return { kind: 'failed', reason: PUBLISH_NETWORK_ERROR }
      return { kind: 'processing', containerId: id }
    }
```

- [ ] **Step 2: YouTube — `thumbnails.set` tras el insert**

En `youtube.ts`, agregar junto a los helpers:

```ts
/**
 * Best-effort a conciencia: cuando esto corre el video YA subió, así que un fallo
 * aquí (canal sin teléfono verificado → 403, portada ilegible, red) solo pierde la
 * portada — devolver failed reintentaría el insert y duplicaría el video público.
 * Se llama una sola vez, junto al insert; el camino de reanudación no la repite.
 */
async function setThumbnail(token: string, videoId: string, coverUrl: string): Promise<void> {
  try {
    const cover = await fetch(coverUrl, { signal: AbortSignal.timeout(30_000) })
    if (!cover.ok) {
      console.error('Portada de YouTube:', cover.status, coverUrl.slice(0, 200))
      return
    }
    const response = await fetch(
      `https://www.googleapis.com/upload/youtube/v3/thumbnails/set?videoId=${videoId}`,
      {
        method: 'POST',
        headers: {
          Authorization: `Bearer ${token}`,
          'Content-Type': cover.headers.get('content-type') ?? 'image/jpeg',
        },
        body: new Uint8Array(await cover.arrayBuffer()),
      },
    )
    if (!response.ok) {
      console.error('Portada de YouTube:', response.status, (await response.text()).slice(0, 300))
    }
  } catch (error) {
    console.error('Portada de YouTube:', String(error).slice(0, 300))
  }
}
```

Y en `publish`, la rama del insert cambia su cierre:

```ts
    const data = (await response.json()) as { id?: string }
    if (typeof data.id !== 'string') return { kind: 'failed', reason: YOUTUBE_REJECTED }
    if (input.coverUrl) await setThumbnail(input.token, data.id, input.coverUrl)
    return { kind: 'processing', containerId: data.id }
```

- [ ] **Step 3: Verificar y commitear**

Run: `npx vitest run && npm run lint && npx tsc --noEmit`
Expected: todo verde (sin tests nuevos: ambos caminos son HTTP puro).

```bash
git add src/lib/social/publish/facebook.ts src/lib/social/publish/youtube.ts
git commit -m "Aplica la portada en Facebook (thumb) y YouTube (thumbnails.set)"
```

---

### Task 5: Editor de posts — ver, poner, cambiar y quitar la portada

**Files:**
- Modify: `src/app/admin/(dash)/schedule/[id]/page.tsx`
- Modify: `src/app/admin/(dash)/schedule/[id]/editor.tsx`
- Modify: `src/app/admin/actions.ts` (`updateScheduledPost`)

**Interfaces:**
- Consumes: `PORTADA_NEEDS_VIDEO`, `PORTADA_NOT_IMAGE`, `mediaToBlob`, `mediaTypeFromUrl` (Tasks 1-2). Campos nuevos del form: `keepPortada` (hidden, URL actual si se conserva), `portadaUrl` (input; si viene, gana sobre `keepPortada`).
- Produces: nada aguas abajo.

- [ ] **Step 1: page.tsx pasa la portada**

El `<Editor …>` gana `coverUrl={post.coverUrl}`.

- [ ] **Step 2: editor.tsx — la sección de portada**

Props: `coverUrl: string | null`. Estado: `const [keptCover, setKeptCover] = useState(coverUrl)`. Dentro del `<fieldset>`, después del bloque de Media:

```tsx
          <div>
            <span className="mb-1 block font-mono text-[0.65rem] uppercase tracking-[0.14em] text-fg-faint">
              Portada (solo para video)
            </span>
            {keptCover ? (
              <div className="mb-2 flex items-center gap-3 rounded-xl bg-white/[0.04] p-2">
                <input type="hidden" name="keepPortada" value={keptCover} />
                <Image src={keptCover} alt="" width={48} height={48} unoptimized className="h-12 w-12 rounded object-cover" />
                <span className="flex-1 truncate text-xs text-fg-faint">{keptCover}</span>
                <button type="button" onClick={() => setKeptCover(null)} className="text-xs text-fg-faint hover:text-fg">
                  Quitar
                </button>
              </div>
            ) : null}
            <label className="block text-sm">
              {keptCover ? 'Cambiar por URL' : 'Agregar por URL'}
              <input
                type="text"
                name="portadaUrl"
                placeholder="https://…/portada.jpg"
                className="mt-1 w-full rounded-xl bg-white/[0.05] px-3 py-2 text-xs text-fg outline-none"
              />
            </label>
          </div>
```

- [ ] **Step 3: updateScheduledPost procesa la portada**

Imports: sumar `PORTADA_NEEDS_VIDEO, PORTADA_NOT_IMAGE` al import de
`@/lib/social/publish/batch`.

Tras leer los campos del form existentes:

```ts
  const keepPortada = String(formData.get('keepPortada') ?? '').trim()
  const portadaUrl = String(formData.get('portadaUrl') ?? '').trim()
```

Chequeo temprano (junto a las validaciones previas, antes de subir nada):

```ts
  if (portadaUrl && mediaTypeFromUrl(portadaUrl) === 'video') {
    return { error: PORTADA_NOT_IMAGE }
  }
```

Después de la re-validación final (cuando `finalTypes` ya existe), y antes de
persistir:

```ts
  // La URL nueva gana sobre la conservada; ninguna de las dos = quitarla (null).
  let coverUrl: string | null = keepPortada || null
  if (portadaUrl) {
    const stored = await mediaToBlob(portadaUrl, mediaTypeFromUrl(portadaUrl))
    if (!stored) return { error: 'No se pudo leer una media por URL.' }
    if (stored.mediaType !== 'image') return { error: PORTADA_NOT_IMAGE }
    coverUrl = stored.url
  }
  if (coverUrl && !finalTypes.includes('video')) {
    return { error: PORTADA_NEEDS_VIDEO }
  }
```

NOTA: `finalTypes` vive hoy dentro de la rama `pendingNetworks.length > 0`; muévelo
(el `mediaPlan.order.map(...)`) fuera del `if/else` de validación para que este
chequeo funcione también con todo publicado, sin cambiar las validaciones
existentes. El `.set()` del update del post gana `coverUrl`.

- [ ] **Step 4: Verificar y commitear**

Run: `npx vitest run && npm run lint && npx tsc --noEmit && npx next build`
Expected: todo verde y el build compila.

```bash
git add "src/app/admin/(dash)/schedule/[id]/page.tsx" "src/app/admin/(dash)/schedule/[id]/editor.tsx" src/app/admin/actions.ts
git commit -m "Agrega la portada al editor de posts programados"
```
