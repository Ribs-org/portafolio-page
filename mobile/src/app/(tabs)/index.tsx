import { View } from 'react-native'
import { COLORES } from '../../components/ui'

/** Reemplazado en el Task 4 por la pantalla de Resumen. */
export default function Resumen() {
  return <View style={{ flex: 1, backgroundColor: COLORES.fondo }} />
}
